<?xml version="1.0" encoding="UTF-8"?>
<feed xmlns="http://www.w3.org/2005/Atom" xml:lang="{{ lang }}" xml:base="{{ config.base_url | escape_xml | safe }}">
    <title>{{ config.title }} - Bookmarks</title>
    {%- if config.description %}
    <subtitle>All of my bookmarks.</subtitle>
    {%- endif -%}
    {%- if not feed_url is defined %}
        {%- set feed_url = page.permalink | trim_end_matches(pat="/") %}
    {%- endif %}
    <link href="{{ feed_url | safe }}" rel="self" type="application/atom+xml"/>
    <link href="{{ get_url(path='@/posts/bookmarks/_index.md') }}" rel="alternate"/>
    <author>
        <name>{{ config.extra.author.name }}</name>
        <uri>{{ config.base_url | escape_xml | safe }}</uri>
        <email>{{ config.extra.author.email }}</email>
    </author>
    <generator uri="https://www.getzola.org/">Zola</generator>
    <updated>{{ last_updated | default(value=now()) | date(format="%+") }}</updated>
    <id>{{ get_url(path='@/posts/bookmarks/_index.md') }}</id>
    {%- if not pages is defined %}
        {%- set getpages = get_section(path="posts/bookmarks/_index.md") %}
        {%- set pages = getpages.pages %}
    {%- endif %}
    {%- for p in pages %}
    {%- if not p.relative_path is starting_with("pages/") %}
    <entry xml:lang="{{ p.lang }}">
        <title>{%- if p.title -%}
            {{ p.title | safe }}
        {%- else -%}
            Note from {{ p.date | date(format="%Y-%m-%d at %H:%M", timezone="America/Denver") }}
        {%- endif -%}
        </title>
        <published>{{ p.date | date(format="%+", timezone="America/Denver") }}</published>
        <updated>{{ p.updated | default(value=p.date) | date(format="%+", timezone="America/Denver") }}</updated>
        <link rel="alternate" href="{{ p.permalink | safe }}" type="text/html"/>
        <category term="bookmark" scheme="{{ get_url(path='@/posts/bookmarks/_index.md') }}" label="Bookmark" />
        <id>{{ p.permalink | safe }}</id>
        <content type="html">{{ p.content }}</content>
    </entry>
    {%- endif %}
    {%- endfor %}
</feed>
